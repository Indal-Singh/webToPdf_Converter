services:
  webtopdf:
    container_name: webtopdf
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "3002:3002"
    restart: unless-stopped
    shm_size: "1gb"
    mem_limit: 2g
    cpus: 2